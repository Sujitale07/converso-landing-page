export const metadata = {
    title: "Changelog",
    description: "Stay updated with the latest features, improvements, and fixes we've pushed to Connectapre.",
    alternates: {
        canonical: "/changelog",
    },
};

export default function ChangelogLayout({ children }) {
    return <>{children}</>;
}
